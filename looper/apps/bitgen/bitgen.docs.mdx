---
description: bitgen application component.
labels: ['bitgen', 'app']
---

Configure the app plugin on your workspace:

```json
"ashwanth1109.looper/apps/bitgen": {},
```

### Run the application

You can run your application on a separate port to see it outside of the Bit workspace

```bash
bit run bitgen
```
